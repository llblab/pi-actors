{
  "name": "subagent-merge",
  "async": true,
  "args": [
    "inputs:string",
    "mode:enum(consensus-first,risk-first)",
    "risk_policy:string",
    "output_format:string",
    "model:string",
    "thinking:string",
    "tool_args:string"
  ],
  "defaults": {
    "mode": "consensus-first",
    "risk_policy": "Preserve minority severe findings and mark merger-added claims explicitly.",
    "output_format": "Markdown sections: Summary, Consensus, Minority Findings, Contradictions, Next Actions.",
    "model": "openai-codex/gpt-5.5",
    "thinking": "medium",
    "tool_args": "--no-tools"
  },
  "template": "pi -p --model {model} --thinking {thinking} {tool_args} Merge these subagent outputs using mode {mode}: {inputs}. Risk policy: {risk_policy}. Output format: {output_format}"
}
