{
  "args": [
    "target:path",
    "all:bool",
    "repo:path"
  ],
  "defaults": {
    "target": "recipes",
    "all": "true",
    "repo": "~/.pi/agent/extensions/pi-actors"
  },
  "template": "{repo}/scripts/validate-recipe.mjs {target} {all?--all:}"
}
